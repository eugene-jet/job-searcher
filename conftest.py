# Placing a conftest.py at the repository root puts that root on ``sys.path``
# for the test session, so the tests can ``import scrapers`` / ``analytics`` /
# ``daily_report`` the same way the scripts import each other at runtime.
